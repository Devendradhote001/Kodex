import SkeletonCard from "@/components/local/SkeletonCard";
import React from "react";

const loading = () => {
  return (
    <div>
      <SkeletonCard />
    </div>
  );
};

export default loading;
